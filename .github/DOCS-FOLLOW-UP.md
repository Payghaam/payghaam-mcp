# Docs follow-up

Product docs live in [Payghaam/payghaam-mono](https://github.com/Payghaam/payghaam-mono). This repo is public, so mono clones it on a weekly schedule with no PAT.

Optional secret `DOCS_DISPATCH_TOKEN` (Actions: write on payghaam-mono) pings that watcher on push to `main`.
